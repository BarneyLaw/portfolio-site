
  # Create website layout

  This is a code bundle for Create website layout. The original project is available at https://www.figma.com/design/byRXg7IWuNeqF0zWL2EJC2/Create-website-layout.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  