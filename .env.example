# Base URL of the Go backend API. Leave empty to run the site fully static.
# Consumed by src/lib/api.ts for future dynamic features (live uptime,
# view counts, comments, contact form). The static content collections —
# blog, projects, reviews — need no backend.
PUBLIC_API_BASE_URL=
